package qu.stub

/**
 * Contains some abstraction and utilities (in particular async client stubs) for using gRPC not with classic Protobuf's
 * data representation format but with (Jackson) JSON (already implemented) but compatible with other data
 * representation format and technologies as well (still to develop but already arranged for it).
 */
package object client
